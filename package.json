{
  "name": "attendance-ai-root",
  "version": "1.0.0",
  "description": "Enterprise AI-Powered Multi-Tenant HR, Attendance, Task & Payroll Platform",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "postinstall": "cd backend && npm install --legacy-peer-deps && npx prisma generate && cd ../frontend && npm install --legacy-peer-deps",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "node backend/dist/server.js",
    "seed": "npm --prefix backend run seed",
    "db:push": "npm --prefix backend run prisma:push",
    "db:migrate": "npm --prefix backend run prisma:migrate"
  }
}
